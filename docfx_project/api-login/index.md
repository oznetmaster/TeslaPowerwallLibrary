# Login Library API reference

The API reference section contains the generated .NET reference documentation for the `TeslaPowerwallLibrary.Login` namespace and related types.

This library is a separate, Windows-only helper used to perform an interactive Tesla™ cloud OAuth login. It is not part of the `TeslaPowerwallLibrary` NuGet package; see [Tesla cloud login](../articles/login.md) for background and usage.

Use the navigation pane to browse `TeslaCloudLogin` and the supporting login result and token types.
